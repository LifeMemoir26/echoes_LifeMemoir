## 1. 基线与安全护栏

- [x] 1.1 盘点当前主流程入口（knowledge/interview/generate）并形成基线清单文档
- [x] 1.2 固化迁移约束：仅当前分支、本地开发、不推送云端、不合并分支
- [x] 1.3 为旧编排路径添加可切换开关（新/旧路径切换能力）
- [x] 1.4 定义“行为等价”判定口径（输出字段、错误码、关键副作用）
- [x] 1.5 准备基线样本集与回放脚本（覆盖正常/异常/高并发场景）

## 2. DDD 目录骨架与依赖守卫

- [x] 2.1 创建 `src/domain`、`src/application`、`src/infra`、`src/interfaces` 目录骨架
- [x] 2.2 建立跨层依赖规则配置（禁止 `domain` 依赖外层）
- [x] 2.3 增加自动化依赖检查任务并接入本地验证流程
- [x] 2.4 定义跨层 DTO 与错误模型基础结构（含 `trace_id`）
- [x] 2.5 对现有模块进行第一轮映射（旧路径 -> 新分层归属）

## 3. LLM Runtime Gateway 迁移

- [x] 3.1 从现有并发管理器抽取统一网关接口（请求/响应契约）
- [x] 3.2 迁移 key 轮询语义并保持默认行为一致
- [x] 3.3 迁移 429 冷却机制并加入可观测日志字段
- [x] 3.4 迁移重试退避与超时策略（节点级/流程级）
- [x] 3.5 增加取消传播能力，确保取消后释放并发配额
- [x] 3.6 输出并发指标快照接口（成功率、延迟、重试、冷却事件）

## 4. LangGraph 核心框架搭建

- [x] 4.1 建立 workflow 通用基类（状态、节点注册、编译入口）
- [x] 4.2 定义通用状态字段（`workflow_id/thread_id/status/errors/metadata`）
- [x] 4.3 建立 reducer 注册机制并添加缺失 reducer 保护
- [x] 4.4 接入 checkpointer 并支持稳定 `thread_id` 恢复
- [x] 4.5 定义统一错误模型映射（`error_code/failed_node/retryable/trace_id`）

## 5. Interview 工作流迁移（第一阶段）

- [x] 5.1 识别 interview 旧编排节点边界并形成节点列表
- [x] 5.2 实现 `ingest/split_or_buffer` 节点并接入状态契约
- [x] 5.3 实现并行节点：`summarize`、`enrich_pending_events`、`build_context`
- [x] 5.4 实现并行汇合 reducer，验证无覆盖冲突
- [x] 5.5 接入 `persist` 节点并记录失败补偿路径
- [x] 5.6 用旧路径样本对比新路径结果并生成差异报告

## 6. Knowledge 与 Generate 工作流迁移（第二阶段）

- [ ] 6.1 迁移 knowledge 主流程到 `StateGraph`（提取/精炼/向量化）
- [ ] 6.2 为 knowledge 流程补齐状态字段与错误分支
- [ ] 6.3 迁移 generate 主流程到 `StateGraph`
- [ ] 6.4 统一三条主流程的 workflow facade 调用方式
- [ ] 6.5 完成三条主流程的新旧路径 A/B 行为对比

## 7. LangGraph 可视化与可观测细节

- [ ] 7.1 为每条 workflow 生成 Mermaid 拓扑图并保存产物
- [ ] 7.2 生成 PNG 图用于静态审阅（节点/边清晰可读）
- [ ] 7.3 增加运行时节点轨迹记录（开始/结束/错误/重试）
- [ ] 7.4 增加 thread_id 级别执行追踪查询接口
- [ ] 7.5 输出“节点内部细节”调试报告模板（输入摘要/输出摘要/耗时）

## 8. 冗余清理与命名收敛

- [ ] 8.1 建立重复模块清单（含权威模块、替代路径、风险说明）
- [ ] 8.2 清理同义命名文件并保留单一权威实现
- [ ] 8.3 移除已迁移流程的旧手工编排入口
- [ ] 8.4 统一目录与模块命名规范并补充检查脚本
- [ ] 8.5 为每次删除操作记录回滚方案与影响范围

## 9. 性能优化与并发压测

- [ ] 9.1 建立性能基线（吞吐、P95、错误率、重试率）
- [ ] 9.2 对并发上限、超时、重试参数做分批调优实验
- [ ] 9.3 增加失败注入测试（429、网络抖动、存储超时）
- [ ] 9.4 执行高并发压测并输出对比报告（旧 vs 新）
- [ ] 9.5 验证资源占用（CPU/内存/队列深度）无异常增长

## 10. 验收、回滚演练与收口

- [ ] 10.1 完成全量行为回归并确认 API 契约保持不变
- [ ] 10.2 完成 DDD 分层合规审计并输出违规清零报告
- [ ] 10.3 完成 checkpoint 恢复演练并记录成功率
- [ ] 10.4 完成回滚演练（阶段回滚与局部回滚）并验证恢复可用
- [ ] 10.5 产出最终迁移报告（架构图、性能报告、冗余清理清单、后续建议）
